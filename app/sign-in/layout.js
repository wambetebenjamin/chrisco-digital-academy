export const metadata = {
 title: "Sign In",
 description: "Sign in to CHRISCO Digital Academy to track your courses, certificates and learning progress.",
}

export default function SignInLayout({ children }) {
 return children
}
